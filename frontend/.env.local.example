# Points the frontend at the Express API. Kept as a public env var (NEXT_PUBLIC_)
# because it's read in the browser, not just at build time — there's no secret
# here, just a URL, so that's the correct/safe use of NEXT_PUBLIC_.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
